"""Shared, ROS-free perception primitives for the Puzzlebot line follower.

These modules are imported by both the runtime node
(``puzzlebot_ros/line_follower.py``) and the offline calibrator
(``tools/line_vision_calibrator.py``) so the two never diverge again.
"""
