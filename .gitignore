build/
install/
log/
__pycache__/
*.py[cod]
.pytest_cache/

# Runtime/debug outputs from Jetson tests.
puzzlebot_ros/line_debug/
puzzlebot_ros/*_plot.png
puzzlebot_ros/controller_data.csv

# Raw calibration captures — kept locally to recompute, not in git.
# The computed results in config/*.npz ARE versioned.
calibration_images/
illumination_samples/
config/undistorted_preview.jpg
config/illumination_preview.jpg

# Calibration captures pulled from the Jetson (pull_calibration_dataset.sh
# still lands here locally; recreated on each pull).
debug_dataset/
.claude/

# Datasets — store in Drive/Roboflow, not in git. All datasets live under
# datasets/ now: signs/, traffic_light/, calibration/ (e.g. tilt_session).
datasets/
dataset*.zip

# Per-laptop script overrides. Copy scripts/local.env.example to scripts/local.env.
scripts/local.env
